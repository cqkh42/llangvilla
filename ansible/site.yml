- hosts: 
  - optiplex
  tasks: 
    - name: Set basic env vars
      debug:
        msg: hello
    - name: Install packages
      ansible.builtin.apt:
        name:
          - curl
          - git
          - qemu-kvm
        state: present
        update_cache: yes
